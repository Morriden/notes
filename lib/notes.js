const execute = action => {
    // series of ifs
    //switch
    // dictionary of functions
};

const add = obj => {
    let note = {
        text: obj.payload
    };
    return note;
    // console.log(note);
};

module.exports = {
    execute,
    add  
};